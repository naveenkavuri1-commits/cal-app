<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.compkncalculator">

    <application
        android:allowBackup="true"
        android:label="@string/compkn_app_name"
        android:icon="@android:drawable/sym_def_app_icon"
        android:roundIcon="@android:drawable/sym_def_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.compknCalculator">

        <activity
            android:name=".compknConverterDetailActivity"
            android:exported="false" />

        <activity
            android:name=".compknScientificActivity"
            android:exported="false" />

        <activity
            android:name=".compknMainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>